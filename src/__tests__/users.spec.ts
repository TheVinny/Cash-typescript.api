import app from '@shared/infra/http/server';
import request from 'supertest';

describe('Users', () => {
  it('Should be able authenticate ?', async () => {
    //falta criar as outras migrations
    //e ajustar os scripts para dropar todos as tabelas
  });
});
